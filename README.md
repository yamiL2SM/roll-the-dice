"# roll-the-dice" 
A simple console-based number guessing game written in Java. The computer randomly selects a number between 1 and 100, and the player attempts to guess it with hints provided after each attempt.

Features.
Random number generation (1-100);
Interactive hints for higher/lower guesses;
Attempt counter;
Result tracking with best game statistics;
Special RESULT command to display statistics;
